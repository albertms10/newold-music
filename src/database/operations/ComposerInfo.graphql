query ComposerInfo($id: Int!) {
  composers_by_pk(id: $id) {
    id
    name
    surname
    roled_composers {
      composer_role {
        id
        role
      }
      work_roled_composers {
        work {
          id
          duration
          title
        }
      }
    }
  }
}
